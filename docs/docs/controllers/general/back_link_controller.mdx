---
id: BackLinkController
title: BackLinkController
---


import NoActions from "../../_partials/no-actions.md";
import NoTargets from "../../_partials/no-targets.md";
import NoClasses from "../../_partials/no-classes.md";
import NoEvents from "../../_partials/no-events.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

A controller to enable a link to navigate back 1 page using the user's browser history. Falls back to the `href` of the link if there is no history available or the operation fails.

## [Actions](https://stimulus.hotwire.dev/reference/actions)
<NoActions/>

## [Targets](https://stimulus.hotwire.dev/reference/targets)
<NoTargets/>

## [Classes](https://stimulus.hotwire.dev/reference/classes)
<NoClasses/>

## [Values](https://stimulus.hotwire.dev/reference/values)

| Value     | Type   | Description                                                                                                                   | Default |
|-----------|--------|-------------------------------------------------------------------------------------------------------------------------------|---------|
| `timeout` | Number | The delay in `ms` before the controller should give up on history navigation and fall back to navigating to the link's `href` | 1500    |
| `pages`   | Number | The number of entries in the browser history to navigate back by                                                              | 1       |

## Events
<NoEvents/>

## Side Effects

- Adds event listeners to the attached element for `click`
- When a click occurs, if there is an `href` present on the link: sets a `setTimeout` for `timeoutValue` milliseconds so that if navigation fails, the page will instead navigate to the `href` of the link.

## How to Use

<StimulusPlayground
  controller="BackLinkController"
  html={`<div style="display:flex;flex-direction:column;gap:0.75rem;max-width:360px;">
  <p style="font-size:0.9rem;color:#374151;margin:0;">
    Click the link to go back in browser history. If there is no previous page, the controller
    falls back to navigating to the link's <code>href</code>.
  </p>
  <a href="#fallback" data-controller="back-link"
    style="display:inline-flex;align-items:center;gap:0.5rem;padding:0.5rem 1rem;background:#6366f1;color:#fff;border-radius:6px;text-decoration:none;font-size:0.9rem;width:fit-content;">
    &#8592; Go back
  </a>
  <p style="font-size:0.85rem;color:#9ca3af;margin:0;">
    Note: <code>history.back()</code> has no effect in a sandboxed preview with no prior history.
    The fallback href would be followed after the timeout (default 1500ms).
  </p>
</div>`}
  editorHeight={200}
/>
