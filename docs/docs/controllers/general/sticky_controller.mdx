---
id: StickyController
title: StickyController
---


import NoActions from "../../_partials/no-actions.md";
import NoTargets from "../../_partials/no-targets.md";
import NoEvents from "../../_partials/no-events.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

When a sticky element (that is, an element styled with CSS `position: sticky` - is "stuck", it is permanently in the viewport, we sometimes need a way to know when the viewport reaches/leaves
the "true" position of the element in the DOM to enable styling of sticky elements one way when they are fixed (or unstuck), and another way when they are sticky/stuck.

### Example Use Cases
- For a sticky navbar, you might want to know when the viewport leaves the bottom of the sticky element's "true" position so that you can shrink the navbar to be less obtrusive.
- For a sticky "action" bar, or bottom nav-bar, you want to know when the top of the element is entering/leaving the viewport so that you can inline it, instead of having it permanently stuck and obscuring end-of-page content.


## [Actions](https://stimulus.hotwire.dev/reference/actions)
<NoActions/>

## [Targets](https://stimulus.hotwire.dev/reference/targets)
<NoTargets/>

## [Classes](https://stimulus.hotwire.dev/reference/classes)

| Class              | Purpose                                                                                 | Default |
|--------------------|-----------------------------------------------------------------------------------------|---------|
| `stuck` (Optional) | The class to apply the the sticky element when it is "stuck", and following the window. | `stuck` |


## [Values](https://stimulus.hotwire.dev/reference/values)

| Value  | Type   | Description                                                                                                                 | Default |
|--------|--------|-----------------------------------------------------------------------------------------------------------------------------|---------|
| `mode` | String | One of `top` or `bottom`, as the position of where the magic element will be inserted relative to the bound sticky element. | `top`   |


## Events
<NoEvents/>

## Side Effects

Inserts a `magic` empty `<DIV>` element into the DOM adjacent to the element the controller is attached to, that is observed with an `IntersectionObserver`.


## How to Use

This controller, when attached to a sticky element, inserts an empty `magic` div element into the DOM. One that has no styles or content.
It is inserted either before or after the controller root element, based on `modeValue`, and then observed with an `IntersectionObserver`.

When the `magic` element is in the view, because it is directly adjacent to the sticky element's real position in the DOM, we know the
sticky element is "unstuck". When the element leaves the view, we know the sticky element is "stuck".

We can then apply CSS classes to take advantage of this.


<StimulusPlayground
  controller="StickyController"
  html={`<style>
  .stuck { background: #4f46e5 !important; color: #fff !important; box-shadow: 0 2px 8px rgba(0,0,0,0.15); }
  .stuck .stuck-indicator { display: inline; }
  .stuck-indicator { display: none; }
</style>
<div style="height:280px;overflow-y:auto;border:1px solid #e5e7eb;border-radius:8px;">
  <div style="height:120px;padding:1rem;background:#f9fafb;display:flex;align-items:center;justify-content:center;color:#9ca3af;font-size:0.9rem;">
    Scroll down
  </div>
  <div data-controller="sticky"
    data-sticky-mode-value="top"
    data-sticky-stuck-class="stuck"
    style="position:sticky;top:0;padding:0.6rem 1rem;background:#eef2ff;border-bottom:1px solid #c7d2fe;font-weight:500;color:#4f46e5;transition:all 0.2s;z-index:10;">
    Sticky header <span class="stuck-indicator">(stuck!)</span>
  </div>
  <div style="padding:1rem;display:flex;flex-direction:column;gap:0.75rem;">
    <p style="margin:0;color:#374151;font-size:0.9rem;">Scroll down to see the header get the "stuck" class applied.</p>
    <p style="margin:0;color:#374151;font-size:0.9rem;">The class changes the background to indigo when the header is pinned to the top.</p>
    <p style="margin:0;color:#374151;font-size:0.9rem;">Keep scrolling to confirm the style change.</p>
    <p style="margin:0;color:#374151;font-size:0.9rem;">Scroll back up to see it return to normal.</p>
  </div>
</div>`}
  editorHeight={300}
/>
