---
id: PrefetchController
title: PrefetchController
---


import NoEvents from "../../_partials/no-events.md";
import NoTargets from "../../_partials/no-targets.md";
import NoActions from "../../_partials/no-actions.md";
import NoClasses from "../../_partials/no-classes.md";
import NoSideEffects from "../../_partials/no-side-effects.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

Preload links when they come into view, or the user hovers over them.

## [Actions](https://stimulus.hotwire.dev/reference/actions)
<NoActions/>

## [Targets](https://stimulus.hotwire.dev/reference/targets)
<NoTargets/>

## [Classes](https://stimulus.hotwire.dev/reference/classes)
<NoClasses/>

## [Values](https://stimulus.hotwire.dev/reference/values)

| Value  | Type   | Description                                                                                                                                    | Default     |
|--------|--------|------------------------------------------------------------------------------------------------------------------------------------------------|-------------|
| `mode` | String | Which mode to use for prefetching links. `intersect` - When the link comes into the viewport. `mouseover` - When the user hovers over the link | `mouseover` |

## Events
<NoEvents/>

## Side Effects
<NoSideEffects/>

## How to Use

The controller will preload links by injecting a `<link rel="prefetch" as="document" href="...">` element into the document `<head>`.

It will only do so if there is not already a prefetch link with the same `href` already in the `<head>`.

<StimulusPlayground
  controller="PrefetchController"
  html={`<div style="display:flex;flex-direction:column;gap:1rem;max-width:420px;">
  <p style="font-size:0.9rem;color:#374151;margin:0;">
    Hover over either link below. The controller will inject a
    <code>&lt;link rel="prefetch"&gt;</code> into <code>&lt;head&gt;</code> the first time
    you hover.
  </p>
  <div style="display:flex;flex-direction:column;gap:0.5rem;">
    <a href="https://example.com/page-one"
      data-controller="prefetch"
      data-prefetch-mode-value="mouseover"
      style="padding:0.5rem 1rem;background:#eef2ff;border:1px solid #c7d2fe;border-radius:6px;color:#4f46e5;text-decoration:none;font-size:0.9rem;">
      Hover to prefetch: /page-one (mouseover mode)
    </a>
    <a href="https://example.com/page-two"
      data-controller="prefetch"
      data-prefetch-mode-value="intersect"
      style="padding:0.5rem 1rem;background:#f0fdf4;border:1px solid #bbf7d0;border-radius:6px;color:#16a34a;text-decoration:none;font-size:0.9rem;">
      Auto-prefetch on visibility: /page-two (intersect mode)
    </a>
  </div>
  <div id="prefetch-log" style="padding:0.75rem;background:#f9fafb;border:1px solid #e5e7eb;border-radius:6px;font-size:0.85rem;color:#374151;">
    Prefetch links in &lt;head&gt;: <span id="prefetch-count">0</span>
  </div>
</div>`}
  js={`function updateCount() {
  const links = document.head.querySelectorAll('link[rel="prefetch"]');
  document.getElementById('prefetch-count').textContent = links.length;
  if (links.length > 0) {
    const hrefs = Array.from(links).map(l => l.href).join(', ');
    document.getElementById('prefetch-log').innerHTML =
      'Prefetch links in &lt;head&gt;: <strong>' + links.length + '</strong><br><small style="color:#6b7280;">' + hrefs + '</small>';
  }
}
setInterval(updateCount, 500);`}
  editorHeight={250}
/>