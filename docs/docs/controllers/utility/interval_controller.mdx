---
id: IntervalController
title: IntervalController
---


import NoActions from "../../_partials/no-actions.md";
import NoTargets from "../../_partials/no-targets.md";
import NoClasses from "../../_partials/no-classes.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

Utility controller to wire up other controller actions that need to fire at set intervals.


## [Actions](https://stimulus.hotwire.dev/reference/actions)
<NoActions/>

## [Targets](https://stimulus.hotwire.dev/reference/targets)
<NoTargets/>

## [Classes](https://stimulus.hotwire.dev/reference/classes)
<NoClasses/>

## [Values](https://stimulus.hotwire.dev/reference/values)

| Value     | Type   | Description                               | Default |
|-----------|--------|-------------------------------------------|---------|
| `seconds` | Number | The number of seconds between each action | -       |

## Events

| Event             | When                                                                                | Dispatched on               | `event.detail`                         |
|-------------------|-------------------------------------------------------------------------------------|-----------------------------|----------------------------------------|
| `interval:action` | Fired every `secondsValue` seconds, used to wire your desired controller actions to | the controller root element | `element`: the controller root element |

## Side Effects

Calls `setInterval` with the desired time value. Cleans up on disconnect.

## How to Use

An example: Save the progress of your users through a particularly lengthy form every 30 seconds.

```html
<form data-controller="form-save" data-action="interval:action->form-save#save">
  <meta data-controller="interval" data-interval-seconds-value="30" />
</form>
```

<StimulusPlayground
  controller="IntervalController"
  html={`<div style="display:flex;flex-direction:column;gap:0.75rem;max-width:360px;">
  <div data-controller="interval"
    data-interval-seconds-value="1"
    id="ticker">
  </div>
  <div id="counter-display"
    style="font-size:2.5rem;font-weight:700;color:#6366f1;text-align:center;padding:1rem;background:#eef2ff;border-radius:12px;">
    0
  </div>
  <p style="font-size:0.85rem;color:#6b7280;text-align:center;margin:0;">
    Counter increments every second via the <code>interval:action</code> event.
  </p>
</div>`}
  js={`let count = 0;
document.getElementById('ticker').addEventListener('interval:action', () => {
  count++;
  document.getElementById('counter-display').textContent = count;
});`}
  editorHeight={210}
/>
