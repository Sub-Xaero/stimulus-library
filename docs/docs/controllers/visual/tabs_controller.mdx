---
id: TabsController
title: TabsController
---


import NoActions from "../../_partials/no-actions.md";
import NoEvents from "../../_partials/no-events.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

Stimulus controller to enable a tabbed view of a page.

## [Actions](https://stimulus.hotwire.dev/reference/actions)
<NoActions/>

## [Targets](https://stimulus.hotwire.dev/reference/targets)

| Target    | Purpose                                                     | Default |
|-----------|-------------------------------------------------------------|---------|
| `link`    | The tab headings/buttons that will be used to open the tabs | -       |
| `content` | The content panels for each tab.                            | -       |

## [Classes](https://stimulus.hotwire.dev/reference/classes)

| Class               | Purpose                                                        | Default     |
|---------------------|----------------------------------------------------------------|-------------|
| `active` (Optional) | The class that is applied to the current active panel and link | `is-active` |
| `hide` (Optional)   | The class used to hide all inactive content panels             | `hide`      |

## [Values](https://stimulus.hotwire.dev/reference/values)

| Value        | Type    | Description                                                                                                                      | Default |
|--------------|---------|----------------------------------------------------------------------------------------------------------------------------------|---------|
| `currentTab` | Number  | The currently selected tab (zero indexed). Setting the initial value/Changing this at any time will change which panel is shown. | `0`     |
| `equalize`   | Boolean | Whether or not to make all content panels as big as the largest panel in the set.                                                | `false` |

## Events
<NoEvents/>

## Side Effects

The controller will add an event listener for `click` to all `link` targets.

The controller will add `aria-selected` to the currently active `link` target.

## How to Use

<StimulusPlayground
  controller="TabsController"
  html={`<style>
  .tab-btn { padding:0.5rem 1.25rem;border:1px solid #e5e7eb;border-bottom:none;
             background:#f9fafb;cursor:pointer;border-radius:4px 4px 0 0;font-size:0.9rem; }
  .tab-btn.is-active { background:white;font-weight:600;border-bottom:1px solid white;
                       margin-bottom:-1px; }
  .tab-panel { border:1px solid #e5e7eb;padding:1.25rem;border-radius:0 4px 4px 4px; }
  .hide { display:none; }
</style>
<div data-controller="tabs">
  <div style="display:flex;">
    <button data-tabs-target="link" class="tab-btn is-active">Overview</button>
    <button data-tabs-target="link" class="tab-btn">Details</button>
    <button data-tabs-target="link" class="tab-btn">Settings</button>
  </div>
  <div data-tabs-target="content" class="tab-panel">
    <strong>Overview</strong>
    <p>This is the first tab panel. Click a tab above to switch.</p>
  </div>
  <div data-tabs-target="content" class="tab-panel hide">
    <strong>Details</strong>
    <p>This is the second tab panel.</p>
  </div>
  <div data-tabs-target="content" class="tab-panel hide">
    <strong>Settings</strong>
    <p>This is the third tab panel.</p>
  </div>
</div>`}
  editorHeight={320}
/>

