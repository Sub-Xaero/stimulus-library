---
id: NavigateFormErrorsController
title: NavigateFormErrorsController
---


import NoEvents from "../../_partials/no-events.md";
import NoSideEffects from "../../_partials/no-side-effects.md";
import StimulusPlayground from "@site/src/components/StimulusPlayground";

## Purpose

A controller to allow users to navigate between form errors when a submission comes back with failed validations.


## [Actions](https://stimulus.hotwire.dev/reference/actions)

| Action     | Purpose                      |
|------------|------------------------------|
| `current`  | Scroll to the current error  |
| `next`     | Scroll to the next error     |
| `previous` | Scroll to the previous error |


## [Targets](https://stimulus.hotwire.dev/reference/targets)

If these targets are specified, the controller will automatically disable and re-enable the buttons as the user clicks between errors

| Target                | Purpose                                         | Default |
|-----------------------|-------------------------------------------------|---------|
| `current` (Optional)  | The button that navigate to the current error.  | -       |
| `next` (Optional)     | The button that navigate to the next error.     | -       |
| `previous` (Optional) | The button that navigate to the previous error. | -       |


## [Classes](https://stimulus.hotwire.dev/reference/classes)
| Class     | Purpose                                                                                  | Default        |
|-----------|------------------------------------------------------------------------------------------|----------------|
| `current` | The class to apply to the targeted element that the controller is currently pointing at. | `currentError` |


## [Values](https://stimulus.hotwire.dev/reference/values)

| Value      | Type   | Description                                                        | Default |
|------------|--------|--------------------------------------------------------------------|---------|
| `selector` | String | The CSS selector the controller should use to find the form errors | -       |


## Events
<NoEvents/>

## Side Effects
<NoSideEffects/>


## How to Use

There is nothing worse as an end user than filling out a long form, only to be bounced back and have to read back through the whole form to try and find where you missed a field.

If you add the controller and its elements to a header/footer/sticky widget/anywhere you like, when a form submission fails, you can use it as follows to allow the user to jump
back and forth between the fields that failed validation.

If you tell the controller about your `next`, `current` and/or `previous` target then controller will automatically disable and re-enable the buttons as the user hits the first/last error.

If the controller cannot find any errors using the provided selector, it will hide the controller root element using `display: none` so that the user is not shown buttons that do nothing.

<StimulusPlayground
  controller="NavigateFormErrorsController"
  html={`<div data-controller="navigate-form-errors"
  data-navigate-form-errors-selector-value=".field-error"
  data-navigate-form-errors-current-class="ring-2"
  style="max-width:420px;">
  <div style="display:flex;align-items:center;gap:0.5rem;margin-bottom:1rem;padding:0.75rem;background:#fef2f2;border:1px solid #fecaca;border-radius:8px;">
    <span style="color:#dc2626;font-size:0.9rem;font-weight:500;">3 errors found</span>
    <div style="margin-left:auto;display:flex;gap:0.25rem;">
      <button data-navigate-form-errors-target="previous"
        style="padding:0.25rem 0.6rem;background:#e5e7eb;border:none;border-radius:4px;cursor:pointer;font-size:0.85rem;">
        Prev
      </button>
      <button data-navigate-form-errors-target="current"
        style="padding:0.25rem 0.6rem;background:#6366f1;color:#fff;border:none;border-radius:4px;cursor:pointer;font-size:0.85rem;">
        Jump to error
      </button>
      <button data-navigate-form-errors-target="next"
        style="padding:0.25rem 0.6rem;background:#e5e7eb;border:none;border-radius:4px;cursor:pointer;font-size:0.85rem;">
        Next
      </button>
    </div>
  </div>
  <div style="display:flex;flex-direction:column;gap:1rem;overflow-y:auto;max-height:240px;padding:0.25rem;">
    <div>
      <label style="font-size:0.85rem;font-weight:500;color:#374151;">Email</label>
      <input type="text" value="not-an-email"
        style="display:block;width:100%;margin-top:0.25rem;padding:0.4rem;border:1px solid #fca5a5;border-radius:6px;">
      <p class="field-error" style="color:#dc2626;font-size:0.8rem;margin:0.25rem 0 0;">Must be a valid email address.</p>
    </div>
    <div>
      <label style="font-size:0.85rem;font-weight:500;color:#374151;">Password</label>
      <input type="password" value=""
        style="display:block;width:100%;margin-top:0.25rem;padding:0.4rem;border:1px solid #fca5a5;border-radius:6px;">
      <p class="field-error" style="color:#dc2626;font-size:0.8rem;margin:0.25rem 0 0;">Password is required.</p>
    </div>
    <div>
      <label style="font-size:0.85rem;font-weight:500;color:#374151;">Username</label>
      <input type="text" value="a"
        style="display:block;width:100%;margin-top:0.25rem;padding:0.4rem;border:1px solid #fca5a5;border-radius:6px;">
      <p class="field-error" style="color:#dc2626;font-size:0.8rem;margin:0.25rem 0 0;">Must be at least 3 characters.</p>
    </div>
  </div>
</div>`}
  editorHeight={330}
/>


